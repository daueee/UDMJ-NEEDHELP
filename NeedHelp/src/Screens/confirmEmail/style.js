import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    text: {
        fontSize: 20,
        fontWeight: 'bold',
        alignSelf: 'center',
        marginTop: 200
    }

})

export default styles